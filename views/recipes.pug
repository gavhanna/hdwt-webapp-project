extends layout

block content

  h1 Recipes
  .recipes-container
    each recipe in response
      .recipes-card
        .recipes-card-img-container
          img(src=recipe.img_url, alt=recipe.title)
        .recipes-card-content
          h2 #{recipe.title}
          p #{recipe.description}
          p #{recipe.preptime}hrs
          p #{recipe.cooktime}hrs
          a(href="/recipes/" + recipe.id) View